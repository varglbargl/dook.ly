var url = require('url');
var fs = require('fs');
var colors = require('./colorHelpers.js');
var server = require('./personal-server.js');

exports.identifyImages = function (css, results, res) {
  
}